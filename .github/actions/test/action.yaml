name: Tests
description: Setup Python + Poetry and run tests

inputs:
  python-version:
    required: true
  poetry-version:
    required: true

runs: 
  using: composite 
  steps: 
    - uses: actions/setup-python@v5
      with: 
        python-version: ${{ inputs.python-version }}

    - name: Setup Poetry
      uses: abatilo/actions-poetry@v3
      with: 
        poetry-version: ${{ inputs.poetry-version }}

    - name: Configure Poetry
      shell: bash
      run: poetry config virtualenvs.in-project true
        
    - name: Install dependencies
      shell: bash
      run: | 
        poetry install --with test,ray --no-interaction --no-ansi
    
    - name: Run Tests
      shell: bash
      run: make test-coverage